using UnityEngine;
using System.Collections;

public class ResourcesEvents : MonoBehaviour {

    public enum EventType { FOODEVENT, WATEREVENT, HEALTH }

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
